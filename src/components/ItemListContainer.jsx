import React from 'react'

const ItemListContainer = (props) => {
    return (
        <div className="container mt-5">
            <h4 className="text-center">Bienvenidos a {props.nombre}</h4>
        </div>
    )
}

export default ItemListContainer
